// const Events = artifacts.require("Events");

module.exports = async function (deployer, network, accounts) {
  //   await deployer.deploy(ERC20Mock, "Brazilian Real Token", "BRL1", supply);
  //   const erc20Mock = await ERC20Mock.deployed();
};
